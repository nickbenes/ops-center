---
description: /ops-center run <name> against a registered project must report the path and tell the human to cd/reopen there — never claim to have switched into it itself.
max_turns: 4
allowed_tools: [Read, Glob, Grep]
---

/ops-center run eval-fixture-project
